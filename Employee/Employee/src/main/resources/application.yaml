spring:
  application:
    name: Employee
  profiles:
    active: dev

  datasource:
    url: jdbc:postgresql://localhost:5432/employee_db
    username: postgres
    password: Soumyajeet

  jpa:
    hibernate:
      ddl-auto: update

    show-sql: true

    properties:
      hibernate:
        format_sql: true
